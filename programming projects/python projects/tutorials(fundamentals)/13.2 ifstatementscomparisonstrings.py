def object(dog, cat, mouse): # == used when comparing a string
    if dog != cat and  dog != mouse: #!= not equal
        return dog
    elif cat != dog and cat != mouse:
        return cat
    else:
        return mouse

    print(mouse, cat, dog)

#<=
#>=
